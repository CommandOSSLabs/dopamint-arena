name: Deploy Backend

on:
  push:
    branches: [dev-raid]
    paths:
      - "backend/**"
      - "infra/**"
      - ".github/workflows/deploy-backend.yml"
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        required: true
        default: "dev"
        type: choice
        options: [dev, staging, production]

permissions:
  id-token: write
  contents: read

# Shared group with deploy-infra: both run `pulumi up` on the same stack, so they must
# serialize or they collide with a 409 ConcurrentUpdate (Pulumi's per-stack lock).
concurrency:
  group: pulumi-${{ github.event.inputs.environment || 'dev' }}
  cancel-in-progress: false

jobs:
  build-push:
    name: Build and push backend image
    runs-on: ubuntu-24.04-arm
    environment: ${{ github.event.inputs.environment || 'dev' }}
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@eef61447b9ff4aafe5dcd4e0bbf5d482be7e7871 # v4.2.1
        with:
          persist-credentials: false

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ececac1a45f3b08a01d2dd070d28d111c5fe6722 # v4.1.0
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Build and push Docker image
        env:
          ECR_URL: ${{ vars.ECR_URL }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          set -euo pipefail
          aws ecr get-login-password | docker login --username AWS --password-stdin "${ECR_URL}"
          docker build -f backend/tunnel-manager/Dockerfile -t "${ECR_URL}:${IMAGE_TAG}" .
          docker push "${ECR_URL}:${IMAGE_TAG}"

  update-pulumi:
    name: Update Pulumi backend image tag
    needs: build-push
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    timeout-minutes: 30
    defaults:
      run:
        working-directory: infra
    steps:
      - name: Checkout repository
        uses: actions/checkout@eef61447b9ff4aafe5dcd4e0bbf5d482be7e7871 # v4.2.1
        with:
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/action-setup@a15d269cd4658e1107c09f1fabf4cbd7bd1f308a # v4.4.0
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version-file: .nvmrc
          cache: pnpm
          cache-dependency-path: infra/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ececac1a45f3b08a01d2dd070d28d111c5fe6722 # v4.1.0
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Run Pulumi up
        uses: pulumi/actions@cd99a7f8865434dd3532b586a26f9ebea596894f # v5.5.1
        env:
          PULUMI_ACCESS_TOKEN: ${{ secrets.PULUMI_ACCESS_TOKEN }}
        with:
          command: up
          stack-name: CommandOSS/dopamint-arena/${{ github.event.inputs.environment || 'dev' }}
          work-dir: infra
          pulumi-version: 3.243.0
          config-map: |
            dopamint:backend-image-tag:
              value: ${{ github.sha }}

  run-migrations:
    name: Run database migrations
    needs: update-pulumi
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    timeout-minutes: 20
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ececac1a45f3b08a01d2dd070d28d111c5fe6722 # v4.1.0
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Run migrations on Fargate
        env:
          ECS_CLUSTER: ${{ vars.ECS_CLUSTER }}
          ECS_MIGRATION_TASK_DEF: ${{ vars.ECS_MIGRATION_TASK_DEF }}
          PRIVATE_SUBNET_IDS: ${{ vars.PRIVATE_SUBNET_IDS }}
          BACKEND_SECURITY_GROUP_ID: ${{ vars.BACKEND_SECURITY_GROUP_ID }}
        run: |
          set -euo pipefail
          FIRST_SUBNET="${PRIVATE_SUBNET_IDS%%,*}"
          RUN_RESULT=$(aws ecs run-task \
            --cluster "${ECS_CLUSTER}" \
            --task-definition "${ECS_MIGRATION_TASK_DEF}" \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[${FIRST_SUBNET}],securityGroups=[${BACKEND_SECURITY_GROUP_ID}],assignPublicIp=DISABLED}")
          TASK_ARN=$(echo "${RUN_RESULT}" | jq -r '.tasks[0].taskArn // empty')
          FAILURE=$(echo "${RUN_RESULT}" | jq -c '.failures[0] // empty')
          if [ -z "${TASK_ARN}" ]; then
            echo "ECS run-task failed: ${FAILURE}"
            exit 1
          fi
          aws ecs wait tasks-stopped --cluster "${ECS_CLUSTER}" --tasks "${TASK_ARN}"
          TASK_DESC=$(aws ecs describe-tasks \
            --cluster "${ECS_CLUSTER}" \
            --tasks "${TASK_ARN}")
          EXIT_CODE=$(echo "${TASK_DESC}" | jq -r '.tasks[0].containers[0].exitCode // "null"')
          STOP_REASON=$(echo "${TASK_DESC}" | jq -r '.tasks[0].stoppedReason // "unknown"')
          if [ "${EXIT_CODE}" != "0" ]; then
            echo "Migration failed: exitCode=${EXIT_CODE}, stoppedReason=${STOP_REASON}"
            exit 1
          fi
          echo "Migration completed successfully"

  deploy-service:
    name: Deploy backend ECS service
    needs: run-migrations
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'dev' }}
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@eef61447b9ff4aafe5dcd4e0bbf5d482be7e7871 # v4.2.1
        with:
          persist-credentials: false

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@ececac1a45f3b08a01d2dd070d28d111c5fe6722 # v4.1.0
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Update service to latest backend task definition
        env:
          ECS_CLUSTER: ${{ vars.ECS_CLUSTER }}
          ECS_SERVICE: ${{ vars.ECS_SERVICE }}
          ECS_BACKEND_TASK_DEF: ${{ vars.ECS_BACKEND_TASK_DEF }}
        run: |
          set -euo pipefail
          LATEST_BACKEND_TASK_DEF=$(aws ecs describe-task-definition \
            --task-definition "${ECS_BACKEND_TASK_DEF}" \
            --query 'taskDefinition.taskDefinitionArn' --output text)
          aws ecs update-service \
            --cluster "${ECS_CLUSTER}" \
            --service "${ECS_SERVICE}" \
            --task-definition "$LATEST_BACKEND_TASK_DEF" \
            --force-new-deployment

      - name: Wait for service stability
        env:
          ECS_CLUSTER: ${{ vars.ECS_CLUSTER }}
          ECS_SERVICE: ${{ vars.ECS_SERVICE }}
        run: aws ecs wait services-stable --cluster "${ECS_CLUSTER}" --services "${ECS_SERVICE}"

      - name: Smoke test
        env:
          BACKEND_URL: ${{ vars.BACKEND_URL }}
        run: |
          set -euo pipefail
          curl -fsS "${BACKEND_URL}/health/live"
          curl -fsS "${BACKEND_URL}/health/ready"
