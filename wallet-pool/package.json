{
  "name": "wallet-pool",
  "version": "0.1.0",
  "description": "Sealed pool of Sui accounts — create, fund, list, and sign",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test --test-isolation=none \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@mysten/sui": "1.28.1",
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "prettier": "^3.8.4",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3"
  }
}
