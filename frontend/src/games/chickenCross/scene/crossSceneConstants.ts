/** Discrete columns per lane (0 = left edge). Mirrors cross-config CROSS_COLUMN_COUNT. */
export const CROSS_COLUMN_COUNT = 9;

/** Client hop tween duration (ms). Mirrors cross-config CROSS_HOP_MS. */
export const CROSS_HOP_MS = 160;
